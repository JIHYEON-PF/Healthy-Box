$(window).resize(function () {
    // Header와 Main의 간격 조정
    $("main").css({
        "padding-top" : $("header").height()
    });
});


$(document).ready(function () {
    // Header와 Main의 간격 조정
    $("main").css({
        "padding-top" : $("header").height()
    });
});