<?xml version="1.0"?>
<thlogic>
    <attr sel="#header" th:replace="header :: header"/>
    <attr sel="#footer" th:replace="footer :: footer"/>
    <attr sel="#floatingInput" th:value="${userId != null} ? ${userId} : ''"/>
    <attr sel="#rememberId" th:checked="${userId != null} ? true : false"/>
</thlogic>