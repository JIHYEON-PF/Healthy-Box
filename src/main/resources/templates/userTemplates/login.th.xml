<?xml version="1.0"?>
<thlogic>
    <attr sel="#header" th:replace="header :: header"/>
    <attr sel="#footer" th:replace="footer :: footer"/>
    <attr sel="#floatingInput" th:value="${rememberIdCookie != null} ? ${rememberIdCookie.value} : ''"/>
    <attr sel="#rememberId" th:checked="${rememberIdCookie != null} ? true : false"/>
</thlogic>